# History

## vNext

## v0.0.3

- Added `gitlab()`, `twitter()`, and `linkedin()` methods.
- Re-export the `GitlabUser`, `TwitterUser`, and `LinkedinUser` types from `metrics-api-server`.

## v0.0.2

- Initial release: `MetricsApiClient` wrapper.
- Replaced `githubProfile`/`githubRepos`/`githubContributions` with `github()`; added `token`/`lifetime` options.
